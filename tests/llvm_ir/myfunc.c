#include <stdio.h>

// 自定义打印函数
void my_print(const char* msg) {
    printf("Custom print: %s\n", msg);
}

// 自定义加法函数
int my_add(int a, int b) {
    return a + b;
}

// 自定义问候函数
void say_hello() {
    printf("Hello from C function!\n");
}