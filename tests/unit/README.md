# ???????????

??????????? C++ runner??????????

1. ?? `tests/unit/case` ?? `.cl` ??
2. ?? COOL ? LLVM IR ?????????????? `.actual.ll`
3. ? `clang++` ? `.actual.ll` ????????
4. ?????????????????? `.expected.txt`

## ????

- ?????`xxx.cl`
- ?? IR?`xxx.actual.ll`
- ?????`xxx.expected.txt`
- ?? `tests/unit/cases` ???????

## ????/????

runner ??????????????

```sh
cd {app_dir} && ../bin/.i686/lexer {input} | ./parser {input} | ./semant {input} | ./cgen-llvm {input}
clang++ {actual_ir} -L{runtime_dir} -lruntime -Wl,-rpath,{runtime_dir} -o {executable}
{executable}
```

runner ?????? `q
` ???????? `example.cl` ??????????????
??? `app/parser`?`app/semant`?`app/cgen-llvm` ? `lib/runtime/libruntime.so` ???????

## ??

```sh
make build
```

## ??

```sh
make run
```

???????

```sh
./unit_test_runner --filter smoke
```

????????????????

```sh
make test-units
make unit-test example.cl
```

## ????

- `--repo-root <path>`??????????
- `--cases-dir <path>`?????????
- `--compile-ir-cmd <template>`??? IR ??????
- `--link-cmd <template>`?????????
- `--run-cmd <template>`??????????????
- `--filter <text>`?????????
- `--list`??????????
- `--verbose`??????????????
